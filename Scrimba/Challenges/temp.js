function slice(str) {
    //return str.slice(0, 1);
    //return str.slice(0, -1);
    return str.slice(1, str.lenght);
}

//console.log(slice("Test"));

function split(str) {
    const newArray = str.split(" ")
    return newArray;
}

//console.log(split("test this out"));



